// ______________________________________________________
// Generated by sql2java - http://sql2java.sourceforge.net/
// jdbc driver used at code generation time: com.mysql.jdbc.Driver
//
// Please help us improve this tool by reporting:
// - problems and suggestions to
//   http://sourceforge.net/tracker/?group_id=54687
// - feedbacks and ideas on
//   http://sourceforge.net/forum/forum.php?forum_id=182208
// ______________________________________________________

package com.gwt.hris.db;

import java.util.Comparator;


/**
 * Comparator class is used to sort the TbNotificationUiBean objects.
 * @author sql2java
 */
public class TbNotificationUiComparator implements Comparator
{
    /**
     * Holds the field on which the comparison is performed.
     */
    private int iType;
    /**
     * Value that will contain the information about the order of the sort: normal or reversal.
     */
    private boolean bReverse;

    /**
     * Constructor class for TbNotificationUiComparator.
     * <br>
     * Example:
     * <br>
     * <code>Arrays.sort(pArray, new TbNotificationUiComparator(TbNotificationUiManager.ID_TBNU_NAME, bReverse));<code>
     *
     * @param iType the field from which you want to sort
     * <br>
     * Possible values are:
     * <ul>
     *   <li>TbNotificationUiManager.ID_TBNU_NAME
     *   <li>TbNotificationUiManager.ID_TBNU_ID
     * </ul>
     */
    public TbNotificationUiComparator(int iType)
    {
        this(iType, false);
    }

    /**
     * Constructor class for TbNotificationUiComparator.
     * <br>
     * Example:
     * <br>
     * <code>Arrays.sort(pArray, new TbNotificationUiComparator(TbNotificationUiManager.ID_TBNU_NAME, bReverse));<code>
     *
     * @param iType the field from which you want to sort.
     * <br>
     * Possible values are:
     * <ul>
     *   <li>TbNotificationUiManager.ID_TBNU_NAME
     *   <li>TbNotificationUiManager.ID_TBNU_ID
     * </ul>
     *
     * @param bReverse set this value to true, if you want to reverse the sorting results
     */
    public TbNotificationUiComparator(int iType, boolean bReverse)
    {
        this.iType = iType;
        this.bReverse = bReverse;
    }

    /**
     * Implementation of the compare method.
     */
    public int compare(Object pObj1, Object pObj2)
    {
        TbNotificationUiBean b1 = (TbNotificationUiBean)pObj1;
        TbNotificationUiBean b2 = (TbNotificationUiBean)pObj2;
        int iReturn = 0;
        switch(iType)
        {
            case TbNotificationUiManager.ID_TBNU_NAME:
                if (b1.getTbnuName() == null && b2.getTbnuName() != null) {
                    iReturn = -1;
                } else if (b1.getTbnuName() == null && b2.getTbnuName() == null) {
                    iReturn = 0;
                } else if (b1.getTbnuName() != null && b2.getTbnuName() == null) {
                    iReturn = 1;
                } else {
                    iReturn = b1.getTbnuName().compareTo(b2.getTbnuName());
                }
                break;
            case TbNotificationUiManager.ID_TBNU_ID:
                if (b1.getTbnuId() == null && b2.getTbnuId() != null) {
                    iReturn = -1;
                } else if (b1.getTbnuId() == null && b2.getTbnuId() == null) {
                    iReturn = 0;
                } else if (b1.getTbnuId() != null && b2.getTbnuId() == null) {
                    iReturn = 1;
                } else {
                    iReturn = b1.getTbnuId().compareTo(b2.getTbnuId());
                }
                break;
            default:
                throw new IllegalArgumentException("Type passed for the field is not supported");
        }

        return bReverse ? (-1 * iReturn) : iReturn;
    }}
