// ______________________________________________________
// Generated by sql2java - http://sql2java.sourceforge.net/
// jdbc driver used at code generation time: com.mysql.jdbc.Driver
//
// Please help us improve this tool by reporting:
// - problems and suggestions to
//   http://sourceforge.net/tracker/?group_id=54687
// - feedbacks and ideas on
//   http://sourceforge.net/forum/forum.php?forum_id=182208
// ______________________________________________________

package com.gwt.hris.db;

import java.util.Comparator;


/**
 * Comparator class is used to sort the TbNotificationChannelBean objects.
 * @author sql2java
 */
public class TbNotificationChannelComparator implements Comparator
{
    /**
     * Holds the field on which the comparison is performed.
     */
    private int iType;
    /**
     * Value that will contain the information about the order of the sort: normal or reversal.
     */
    private boolean bReverse;

    /**
     * Constructor class for TbNotificationChannelComparator.
     * <br>
     * Example:
     * <br>
     * <code>Arrays.sort(pArray, new TbNotificationChannelComparator(TbNotificationChannelManager.ID_TBNC_NAME, bReverse));<code>
     *
     * @param iType the field from which you want to sort
     * <br>
     * Possible values are:
     * <ul>
     *   <li>TbNotificationChannelManager.ID_TBNC_NAME
     *   <li>TbNotificationChannelManager.ID_TBNC_ID
     * </ul>
     */
    public TbNotificationChannelComparator(int iType)
    {
        this(iType, false);
    }

    /**
     * Constructor class for TbNotificationChannelComparator.
     * <br>
     * Example:
     * <br>
     * <code>Arrays.sort(pArray, new TbNotificationChannelComparator(TbNotificationChannelManager.ID_TBNC_NAME, bReverse));<code>
     *
     * @param iType the field from which you want to sort.
     * <br>
     * Possible values are:
     * <ul>
     *   <li>TbNotificationChannelManager.ID_TBNC_NAME
     *   <li>TbNotificationChannelManager.ID_TBNC_ID
     * </ul>
     *
     * @param bReverse set this value to true, if you want to reverse the sorting results
     */
    public TbNotificationChannelComparator(int iType, boolean bReverse)
    {
        this.iType = iType;
        this.bReverse = bReverse;
    }

    /**
     * Implementation of the compare method.
     */
    public int compare(Object pObj1, Object pObj2)
    {
        TbNotificationChannelBean b1 = (TbNotificationChannelBean)pObj1;
        TbNotificationChannelBean b2 = (TbNotificationChannelBean)pObj2;
        int iReturn = 0;
        switch(iType)
        {
            case TbNotificationChannelManager.ID_TBNC_NAME:
                if (b1.getTbncName() == null && b2.getTbncName() != null) {
                    iReturn = -1;
                } else if (b1.getTbncName() == null && b2.getTbncName() == null) {
                    iReturn = 0;
                } else if (b1.getTbncName() != null && b2.getTbncName() == null) {
                    iReturn = 1;
                } else {
                    iReturn = b1.getTbncName().compareTo(b2.getTbncName());
                }
                break;
            case TbNotificationChannelManager.ID_TBNC_ID:
                if (b1.getTbncId() == null && b2.getTbncId() != null) {
                    iReturn = -1;
                } else if (b1.getTbncId() == null && b2.getTbncId() == null) {
                    iReturn = 0;
                } else if (b1.getTbncId() != null && b2.getTbncId() == null) {
                    iReturn = 1;
                } else {
                    iReturn = b1.getTbncId().compareTo(b2.getTbncId());
                }
                break;
            default:
                throw new IllegalArgumentException("Type passed for the field is not supported");
        }

        return bReverse ? (-1 * iReturn) : iReturn;
    }}
