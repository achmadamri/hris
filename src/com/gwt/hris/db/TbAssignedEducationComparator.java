// ______________________________________________________
// Generated by sql2java - http://sql2java.sourceforge.net/
// jdbc driver used at code generation time: com.mysql.jdbc.Driver
//
// Please help us improve this tool by reporting:
// - problems and suggestions to
//   http://sourceforge.net/tracker/?group_id=54687
// - feedbacks and ideas on
//   http://sourceforge.net/forum/forum.php?forum_id=182208
// ______________________________________________________

package com.gwt.hris.db;

import java.util.Comparator;


/**
 * Comparator class is used to sort the TbAssignedEducationBean objects.
 * @author sql2java
 */
public class TbAssignedEducationComparator implements Comparator
{
    /**
     * Holds the field on which the comparison is performed.
     */
    private int iType;
    /**
     * Value that will contain the information about the order of the sort: normal or reversal.
     */
    private boolean bReverse;

    /**
     * Constructor class for TbAssignedEducationComparator.
     * <br>
     * Example:
     * <br>
     * <code>Arrays.sort(pArray, new TbAssignedEducationComparator(TbAssignedEducationManager.ID_TBAE_END_DATE, bReverse));<code>
     *
     * @param iType the field from which you want to sort
     * <br>
     * Possible values are:
     * <ul>
     *   <li>TbAssignedEducationManager.ID_TBAE_END_DATE
     *   <li>TbAssignedEducationManager.ID_TBAE_START_DATE
     *   <li>TbAssignedEducationManager.ID_TBAE_MAJOR
     *   <li>TbAssignedEducationManager.ID_TBAE_GPA
     *   <li>TbAssignedEducationManager.ID_TBEDU_ID
     *   <li>TbAssignedEducationManager.ID_TBEMP_ID
     *   <li>TbAssignedEducationManager.ID_TBAE_ID
     * </ul>
     */
    public TbAssignedEducationComparator(int iType)
    {
        this(iType, false);
    }

    /**
     * Constructor class for TbAssignedEducationComparator.
     * <br>
     * Example:
     * <br>
     * <code>Arrays.sort(pArray, new TbAssignedEducationComparator(TbAssignedEducationManager.ID_TBAE_END_DATE, bReverse));<code>
     *
     * @param iType the field from which you want to sort.
     * <br>
     * Possible values are:
     * <ul>
     *   <li>TbAssignedEducationManager.ID_TBAE_END_DATE
     *   <li>TbAssignedEducationManager.ID_TBAE_START_DATE
     *   <li>TbAssignedEducationManager.ID_TBAE_MAJOR
     *   <li>TbAssignedEducationManager.ID_TBAE_GPA
     *   <li>TbAssignedEducationManager.ID_TBEDU_ID
     *   <li>TbAssignedEducationManager.ID_TBEMP_ID
     *   <li>TbAssignedEducationManager.ID_TBAE_ID
     * </ul>
     *
     * @param bReverse set this value to true, if you want to reverse the sorting results
     */
    public TbAssignedEducationComparator(int iType, boolean bReverse)
    {
        this.iType = iType;
        this.bReverse = bReverse;
    }

    /**
     * Implementation of the compare method.
     */
    public int compare(Object pObj1, Object pObj2)
    {
        TbAssignedEducationBean b1 = (TbAssignedEducationBean)pObj1;
        TbAssignedEducationBean b2 = (TbAssignedEducationBean)pObj2;
        int iReturn = 0;
        switch(iType)
        {
            case TbAssignedEducationManager.ID_TBAE_END_DATE:
                if (b1.getTbaeEndDate() == null && b2.getTbaeEndDate() != null) {
                    iReturn = -1;
                } else if (b1.getTbaeEndDate() == null && b2.getTbaeEndDate() == null) {
                    iReturn = 0;
                } else if (b1.getTbaeEndDate() != null && b2.getTbaeEndDate() == null) {
                    iReturn = 1;
                } else {
                    iReturn = b1.getTbaeEndDate().compareTo(b2.getTbaeEndDate());
                }
                break;
            case TbAssignedEducationManager.ID_TBAE_START_DATE:
                if (b1.getTbaeStartDate() == null && b2.getTbaeStartDate() != null) {
                    iReturn = -1;
                } else if (b1.getTbaeStartDate() == null && b2.getTbaeStartDate() == null) {
                    iReturn = 0;
                } else if (b1.getTbaeStartDate() != null && b2.getTbaeStartDate() == null) {
                    iReturn = 1;
                } else {
                    iReturn = b1.getTbaeStartDate().compareTo(b2.getTbaeStartDate());
                }
                break;
            case TbAssignedEducationManager.ID_TBAE_MAJOR:
                if (b1.getTbaeMajor() == null && b2.getTbaeMajor() != null) {
                    iReturn = -1;
                } else if (b1.getTbaeMajor() == null && b2.getTbaeMajor() == null) {
                    iReturn = 0;
                } else if (b1.getTbaeMajor() != null && b2.getTbaeMajor() == null) {
                    iReturn = 1;
                } else {
                    iReturn = b1.getTbaeMajor().compareTo(b2.getTbaeMajor());
                }
                break;
            case TbAssignedEducationManager.ID_TBAE_GPA:
                if (b1.getTbaeGpa() == null && b2.getTbaeGpa() != null) {
                    iReturn = -1;
                } else if (b1.getTbaeGpa() == null && b2.getTbaeGpa() == null) {
                    iReturn = 0;
                } else if (b1.getTbaeGpa() != null && b2.getTbaeGpa() == null) {
                    iReturn = 1;
                } else {
                    iReturn = b1.getTbaeGpa().compareTo(b2.getTbaeGpa());
                }
                break;
            case TbAssignedEducationManager.ID_TBEDU_ID:
                if (b1.getTbeduId() == null && b2.getTbeduId() != null) {
                    iReturn = -1;
                } else if (b1.getTbeduId() == null && b2.getTbeduId() == null) {
                    iReturn = 0;
                } else if (b1.getTbeduId() != null && b2.getTbeduId() == null) {
                    iReturn = 1;
                } else {
                    iReturn = b1.getTbeduId().compareTo(b2.getTbeduId());
                }
                break;
            case TbAssignedEducationManager.ID_TBEMP_ID:
                if (b1.getTbempId() == null && b2.getTbempId() != null) {
                    iReturn = -1;
                } else if (b1.getTbempId() == null && b2.getTbempId() == null) {
                    iReturn = 0;
                } else if (b1.getTbempId() != null && b2.getTbempId() == null) {
                    iReturn = 1;
                } else {
                    iReturn = b1.getTbempId().compareTo(b2.getTbempId());
                }
                break;
            case TbAssignedEducationManager.ID_TBAE_ID:
                if (b1.getTbaeId() == null && b2.getTbaeId() != null) {
                    iReturn = -1;
                } else if (b1.getTbaeId() == null && b2.getTbaeId() == null) {
                    iReturn = 0;
                } else if (b1.getTbaeId() != null && b2.getTbaeId() == null) {
                    iReturn = 1;
                } else {
                    iReturn = b1.getTbaeId().compareTo(b2.getTbaeId());
                }
                break;
            default:
                throw new IllegalArgumentException("Type passed for the field is not supported");
        }

        return bReverse ? (-1 * iReturn) : iReturn;
    }}
